# URL backend API (без слэша в конце). Перед npm run build / npm run deploy должен указывать на боевой API.
# Локально: http://localhost:3001
# Прод (Render): https://kidsfacebook.onrender.com
# VITE_API_URL=http://localhost:3001

# Цена генерации книги в рублях (для будущего VK Pay). 0 = всегда бесплатно.
VITE_BOOK_GENERATION_PRICE_RUB=0
